# Scripts de Utilidad

Estos scripts Node.js se usaron para generar el archivo `assets/plan_lectura.json`
a partir del Excel con el plan anual de lectura bíblica.

## Uso

```bash
cd scripts
npm install
node generate_plan.js
```

El output se copia manualmente a `assets/plan_lectura.json`.
